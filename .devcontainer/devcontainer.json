// Use this version if 'languageserver' is already built into your image
{
  "name": "Custom R Tidyverse Container",
  "image": "voldog/250829_rstudio",
  "workspaceFolder": "/data",
  "shutdownAction": "none",
  "customizations": {
    "vscode": {
      "extensions": [
        "reditorsupport.r"
      ]
    }
  }
}